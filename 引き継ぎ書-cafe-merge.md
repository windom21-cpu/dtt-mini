# 引き継ぎ書 — カフェマージ (cafe-merge)

最終更新: 2026-05-03 / 現行バージョン: v1.0.0

---

## 概要

グルメ・ジャーニーの広告で有名だった「カクテルをスライドして合体させるミニゲーム」(本編からは削除済) のオマージュ。題材をカフェドリンクにして、横1列のレーン上で同じドリンクを合体させながらお題のドリンクを作るパズル。HTML 1 ファイル(CSS / JS インライン)で完結、外部依存なし。

公開URL: https://windom21-cpu.github.io/dtt-mini/cafe-merge/ (push後)

---

## ファイル構成

```
dtt-mini/
├─ cafe-merge/
│  └─ index.html   ← ゲーム本体(これだけで動く)
└─ 引き継ぎ書-cafe-merge.md   ← このファイル
```

---

## 技術スタック

- 純粋な HTML5 + CSS + JavaScript (バニラ JS)
- 描画: DOM + インラインSVG (キャンバスは不使用)
- 永続化: なし
- ホスティング: GitHub Pages (`main` ブランチ直下、`/cafe-merge/` 配下)

ビルド工程なし。`index.html` を編集して push するだけで反映 (1〜2分)。

---

## ゲーム仕様

### コア機構
- 横1列・**7マス**のレーン
- 「NEXT(プレビュー)」スロットに次のドリンクが表示される
- スライド操作で NEXT のドリンクがレーンの右端 (手前側) に入り、そのまま左 (奥) へスライド
- スライド中、隣接するドリンクが**同じ種類**なら合体して1段上のランクに昇格
- 合体直後にさらに左隣と同じなら**連鎖合体**

### 9段階のドリンクツリー
1. エスプレッソ
2. ドリップコーヒー
3. カフェラテ
4. カプチーノ
5. アイスティー
6. アイスカフェモカ
7. フラペチーノ
8. メガパフェドリンク (最上位)

(配列インデックス0は未使用、1〜8 が表示対象)

NEXT に出るのは基本 Lv1 (65%)、たまに Lv2 (27%)、まれに Lv3 (8%)。

### HOLD
- NEXT のドリンクを保管(または保管中のものと交換)
- **1 ターンに 1 回**だけ使用可。スライドすればまた使える

### ステージ制ミッション
全 9 ステージ。クリア条件は「お題のドリンクを N 個作る」、制限ターン内に達成。

| Stage | お題 | 個数 | ターン |
|---|---|---|---|
| 1 | カフェラテ (Lv3) | 1 | 14 |
| 2 | カプチーノ (Lv4) | 1 | 16 |
| 3 | カプチーノ (Lv4) | 2 | 20 |
| 4 | アイスティー (Lv5) | 1 | 20 |
| 5 | アイスティー (Lv5) | 2 | 24 |
| 6 | アイスカフェモカ (Lv6) | 1 | 26 |
| 7 | アイスカフェモカ (Lv6) | 2 | 32 |
| 8 | フラペチーノ (Lv7) | 1 | 34 |
| 9 | メガパフェドリンク (Lv8) | 1 | 44 |

ステージクリアでターンと進捗カウントだけリセット、**盤面 (レーンの内容) は引き継ぐ**。これによって長期戦略(高ランクを温存して次面で活かす)が成立する。

### 終了条件
- ターン切れ (turnsLeft が 0 でクリアできず)
- レーンが詰まって NEXT が入れない (右端マスが空かない)

到達ステージがスコア代わり。

---

## キー / タップ操作

### キーボード
| キー | 動作 |
|---|---|
| Space / ← / Enter | スライド |
| Shift / H / C | HOLD |

### タッチ
画面下部の2ボタン:
- `HOLD` (左、サブ)
- `▶ スライド` (右、プライマリ・幅広)

---

## レイアウト構造

```
┌──────────────────────────────────┐
│ STAGE  残ターン  進捗            │ ← ヘッダー
├──────────────────────────────────┤
│ お題: カプチーノ を 2個 作る     │ ← ミッション
├──────────────────────────────────┤
│ ← 奥                    手前 →   │
│ [ ][ ][ ][ ][ ][ ][ ]           │ ← 7マスレーン
├──────────────────────────────────┤
│      [HOLD] ⇄ [NEXT]            │ ← 待機スロット
├──────────────────────────────────┤
│  [HOLD]      [▶ スライド]        │ ← 操作ボタン
└──────────────────────────────────┘
```

レイアウトは `max-width: 560px` 固定、上から順に縦に並ぶフレックスボックス。スマホ縦持ちが基本想定だが PC でもそのまま動く。

---

## バージョン管理

セマンティックバージョニング:
- パッチ(`1.0.X`): バグ修正・パラメータ調整
- マイナー(`1.X.0`): 機能追加 / レイアウト変更
- メジャー(`X.0.0`): ルールの本質的変更

`index.html` 内の **3箇所** を同期して更新する:
1. `<div id="version">v1.X.Y / build YYYY-MM-DD-NN</div>`
2. JS 冒頭 `const VERSION = 'v1.X.Y';`
3. JS 冒頭 `const BUILD   = 'YYYY-MM-DD-NN';` (NN は同日内の連番、キャッシュ判別用)

build 文字列は console にも出力される。

### バージョン履歴
- v1.0.0 — 初版。レーン7マス・スライド合体・HOLD・全9ステージミッション・PC/タッチ対応・タイトル画面実装

---

## 開発フロー

```bash
# 1. 編集
$EDITOR /home/sk/デスクトップ/dtt-mini/cafe-merge/index.html

# 2. ローカル確認
xdg-open /home/sk/デスクトップ/dtt-mini/cafe-merge/index.html

# 3. バージョン番号を上げる(version文字列・VERSION・BUILDの3箇所)

# 4. コミット & push
cd /home/sk/デスクトップ/dtt-mini
git add cafe-merge/index.html
git commit -m "..."
git push origin main

# 5. 1〜2分後、公開URLで反映確認(右下の version で判別)
```

QRコード生成 (公開後):
```bash
qrencode -o /home/sk/デスクトップ/dtt-mini/cafe-merge/qr.png -s 10 -m 2 \
  "https://windom21-cpu.github.io/dtt-mini/cafe-merge/"
```

---

## コード構造の見取り図

`index.html` 内 JS 部 (約350行):

```
定数
├─ VERSION / BUILD
├─ LANE_SIZE = 7 / MAX_LEVEL = 8
├─ DRINKS                ← 各Lvの表示名
└─ STAGES                ← 各ステージ {target, count, turns}

SVG
└─ svgFor(level)         ← Lv1〜8 の SVG 文字列を返す

ゲーム状態 (グローバル)
└─ board / preview / hold / holdUsed
   stageIdx / turnsLeft / made / busy

ライフサイクル
├─ startGame()           ← 全リセット & タイトル→ゲーム
├─ startStage()          ← ターン・進捗だけリセット(盤面は維持)
├─ stageClear()
└─ gameOver(reason)

ピース操作
├─ randomDrink()         ← Lv1:65% / Lv2:27% / Lv3:8%
├─ slide()               ← preview を右端に投入し左へスライド + 連鎖合体
└─ holdAction()          ← preview と hold を交換 (1ターン1回)

描画
├─ buildLane()           ← LANE_SIZE 個の .cell を一度だけ生成
└─ render()              ← 状態を全DOM要素に反映

入力
├─ start-btn / slide-btn / hold-btn の click
├─ オーバーレイのボタン
└─ keydown: Space/←/Enter=slide, Shift/H/C=hold
```

---

## 既知の制限・改善候補

- ハイスコア(到達ステージ)の保存なし
- BGM・効果音なし
- スライドのアニメーションは「入った瞬間 / 合体した瞬間」のセル単発演出のみ。実際にレーンを横断する移動アニメは未実装
- NEXT の先読みは1個のみ。2〜3個表示して長期戦略を取りやすくする手はある
- レーン7マスはやや窮屈。難ステージで詰まりやすい場合は 8 マス化を検討
- タイトル画面の「最初のステージへの心構え」が伝わりにくい(Stage 1 のお題を先出ししてもよい)
- 各ステージクリア時のオーバーレイで進行できるのに、タイトル戻りボタンが「リセット」を促してしまう導線(現状ボタン2つ並びで紛らわしい — UX 要再考)
- 配色がテーマ統一されすぎて、レベル間のドリンクの**色味の差**が小さく Lv2/Lv3 が一見識別しにくい場面あり

---

## 編集の進め方 (AI協業時の助言)

- ドリンクの絵柄を変えたい → `svgFor(level)` の `case N` を編集。viewBox は 100x100 固定
- ステージを増減 / 難易度調整 → 配列 `STAGES` を編集
- 出現確率を変えたい → `randomDrink()` の閾値
- レーンサイズ変更 → `LANE_SIZE` を変えるだけで描画も追従(CSS は flex)
- 新しい操作を追加 → ボタン要素 + click ハンドラ + `keydown` の分岐に追加
- ターン少残時の警告 → `render()` 内 `turnsLeft <= 3` で `.low` クラス付与済(赤点滅)
